Read the current task and implementation. Add the smallest valuable set of automated tests that prove the acceptance criteria.

Prioritize:

- domain rule tests;
- application use-case tests;
- repository/API integration tests;
- provider-failure and invalid-AI-output tests;
- Android ViewModel/state tests;
- one end-to-end happy path for the vertical slice.

Avoid tests that only mirror implementation or mock every meaningful boundary. Run the tests and report exact commands and results.
