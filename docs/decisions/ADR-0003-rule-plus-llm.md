# ADR-0003：规则引擎与 LLM 分工

状态：Accepted

## Context

完全由 LLM 自由决定计划、掌握和复习会产生不可预测、难测试和难解释的问题；完全使用固定规则又难以生成自然、多样、场景化的语言内容。

## Decision

- 规则/算法负责：优先级、时长预算、到期复习、状态转换、约束校验；
- LLM 负责：题目与对话内容、自然表达、语言分析、总结和解释；
- LLM 输出必须结构化并接受业务规则二次校验。

## Consequences

系统更可测试和可解释，但需要维护规则、Schema、Prompt 和评测集。
